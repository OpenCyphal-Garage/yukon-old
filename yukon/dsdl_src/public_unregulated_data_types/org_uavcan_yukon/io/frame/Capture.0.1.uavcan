# A transport frame captured from the network.

uavcan.time.SynchronizedTimestamp.1.0 timestamp
# Wall time (not monotonic!) when the frame was picked up from the network.

uint64 sequence_number
# The IO worker maintains a frame counter that starts from zero when the interface is initialized.
# Each received frame increments the counter (by virtue of having a very large range it never overflows).

void64

Frame.0.1 frame

@sealed
